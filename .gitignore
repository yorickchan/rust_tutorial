# 建置產出（virtual manifest 的 target/ 在 workspace 根目錄）
/target

# IDE / 編輯器
.idea/
.vscode/
*.swp
*.swo

# macOS
.DS_Store

# 本機工作筆記（session notes，不入版控）
/NOTES.local.md

# EPUB 組裝中間產物（assemble.py 產出，可重新生成）
/ebook/book.md
